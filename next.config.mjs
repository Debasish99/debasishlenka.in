/** @type {import('next').NextConfig} */
const nextConfig = {
  // Photos live in /public/photography and use Next.js's built-in image
  // optimization — no external image host, no remotePatterns needed.
};

export default nextConfig;
